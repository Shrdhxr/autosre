apiVersion: autosre.io/v1
kind: AutoSREIncident
metadata:
  name: test-incident-001
  namespace: default
spec:
  service: crasher
  anomalyType: CRASH_LOOP
  severity: CRITICAL
  diagnosis: "Container exits immediately with code 1, indicating a bad entrypoint command"
  recommendedAction: restart_pod
  confidence: 0.85
status:
  phase: Pending
  approvalState: Waiting